# conf/data/dataset.yaml

mode: local
path: data/dataset        # carpeta local con train/val/test
url: ""                   # si no quieres descargar nada
overwrite: false          # no sobrescribir si ya existe
s3_bucket: food101mini-data  # solo el nombre del bucket


