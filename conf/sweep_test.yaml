defaults:
  - _self_
  - train: train
  - model: mobilenet
  - dataset: dataset
  - outputs: outputs

hydra:
  mode: MULTIRUN
  run:
    dir: outputs/sweep_${now:%Y-%m-%d_%H-%M-%S}

aws:
  use_s3: false            
  region: eu-north-1       
  bucket: food101mini-data 

train:
  batch_size:
    - 16
    - 32