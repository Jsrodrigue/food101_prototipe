defaults:
  - _self_
  - train: train
  - model: efficientnet
  - dataset: dataset
  - outputs: outputs

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d_%H-%M-%S}

aws:
  use_s3: false            
  region: eu-north-1       
  bucket: food101mini-data 